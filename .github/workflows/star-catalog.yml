name: Build star catalog

# Regenerates star-catalog.json (naked-eye star field for night mode) from the
# public-domain HYG database. Run manually — the source is static (J2000), so
# this only needs re-running to change MAG_LIMIT or refresh the data. Branch
# protection blocks direct pushes, so the result arrives as a pull request.
#
# Constellation lines are NOT baked here; they're curated in index.html. If this
# never runs, the app still works from its embedded seed figures.

on:
  workflow_dispatch:
    inputs:
      mag_limit:
        description: "Naked-eye tier limit → star-catalog.json (6.5 ≈ dark-sky eye)"
        required: false
        default: "6.5"
      deep_limit:
        description: "Deep tier limit → star-catalog-deep.json (6.5 dark-sky / 7.0 binocular)"
        required: false
        default: "7.0"

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: star-catalog
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate star catalogs
        env:
          MAG_LIMIT: ${{ github.event.inputs.mag_limit }}
          DEEP_LIMIT: ${{ github.event.inputs.deep_limit }}
        run: node scripts/build-star-catalog.mjs

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: star-catalog
          path: |
            star-catalog.json
            star-catalog-deep.json

      - name: Open PR if changed
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Stage first so brand-new (untracked) catalogs register as a change —
          # plain `git diff` ignores untracked files and would skip the PR.
          git add star-catalog.json star-catalog-deep.json
          if git diff --cached --quiet; then
            echo "No changes to star catalogs"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -B bot/star-catalog
          git commit -m "Update star catalogs from HYG database"
          git push -f origin bot/star-catalog
          # PR creation is best-effort: many repos block Actions from opening PRs
          # ("Allow GitHub Actions to create and approve pull requests" off). The
          # branch is already pushed either way — open the PR by hand if this warns.
          if [ -z "$(gh pr list --head bot/star-catalog --state open --json number -q '.[0].number')" ]; then
            gh pr create --base main --head bot/star-catalog \
              --title "Update star catalogs from HYG database" \
              --body "Automated star-field refresh. Data: HYG database (public domain)." \
              || echo "::warning::Could not open PR automatically — branch bot/star-catalog is pushed; open the PR manually."
          fi
